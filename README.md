# DynamicDockerAgentsbyFasloli
Task — Dynamic Docker Agents on Jenkins Main Goal: configure the main Jenkins controller (defined in docker-compose.yml) to talk to the host Docker engine and use it as a cloud provider for dynamic, ephemeral build agents in a Declarative Pipeline.
